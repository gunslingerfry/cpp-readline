include (CMakeFindDependencyMacro)
# find_dependency/find_project commands need to happen BEFORE the targets
find_dependency(Threads REQUIRED)
include (${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@-targets.cmake)
